apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-ui
spec:
  defaultBackend:
    service:
      name: todo-ui
      port:
        number: 80
  rules:
  - host: localhost
    http:
      paths:
      - backend:
          service:
            name: todo-ui
            port:
              number: 80
        path: /
        pathType: Exact